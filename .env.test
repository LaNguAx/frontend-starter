# Loaded by Vitest (Vite mode "test"). Node's fetch requires absolute URLs, and MSW resolves
# relative handler paths against jsdom's location (http://localhost:3000) — the origins must match.
VITE_API_BASE_URL=http://localhost:3000/api
